<Window x:Class="SMT.CustomRegionCreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Custom Region" Height="420" Width="420" ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Name" />
            <TextBox x:Name="RegionNameBox" Height="22" Margin="0,2,0,6" />

            <TextBlock Text="Template from custom region" />
            <StackPanel Orientation="Horizontal" Margin="0,2,0,6">
                <ComboBox x:Name="TemplateCustomRegionBox" Width="240" />
                <Button Content="Apply" Width="60" Margin="6,0,0,0" Click="ApplyTemplate_Click" />
            </StackPanel>

            <TextBlock Text="Select regions to include" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="2,0,2,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="90" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Include" />
                <TextBlock Grid.Column="1" Text="Region" />
                <TextBlock Grid.Column="2" Text="Type" />
            </Grid>

            <ListView x:Name="RegionList" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ClipToBounds="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" HorizontalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,0,6,0" />
                            <TextBlock Grid.Column="2" Text="{Binding TypeLabel}" HorizontalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Create" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Create_Click" />
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>

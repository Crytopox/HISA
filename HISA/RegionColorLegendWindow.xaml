<Window x:Class="HISA.RegionColorLegendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Region Colors"
        Height="440"
        Width="380"
        MinHeight="360"
        MinWidth="320"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource UiBackgroundBrush}" Style="{StaticResource ModernWindowStyle}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Padding="14" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Region Color Legend" Style="{StaticResource TitleTextStyle}" />
                <TextBlock Text="Color key for the active custom region." Style="{StaticResource SubtitleTextStyle}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="12" Margin="0,0,0,12">
            <ListView x:Name="LegendList" Style="{StaticResource ModernListViewStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0"
                                       Width="16"
                                       Height="16"
                                       Fill="{Binding Brush}"
                                       Stroke="{DynamicResource UiStrokeBrush}"
                                       StrokeThickness="1"
                                       Margin="2"
                                       VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Name}"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource UiTextPrimaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="92" Style="{StaticResource GhostButtonStyle}" Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>

